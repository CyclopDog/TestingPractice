const analyze = (arr = []) => {

  const max = Math.max(arr)
  const min = Math.min(arr)
  const length = arr.length
  const average = 5
  
  const obj = {
    average: average,
    min: min,
    max: max,
    length: length
  }

}

module.exports = analyze;
